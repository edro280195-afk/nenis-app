org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# android_play_install_referrer se compila contra SDK 33 pero trae deps que
# "requieren" 34. Suprimimos el check de AAR metadata (no afecta runtime:
# el plugin funciona bien en 33; las deps nuevas sólo se usan si las llama).
android.suppressUnsupportedCompileSdk=33
