# Progreso — App Flutter compradora (Neni's App · Fase 2)

> Actualizado: 2026-06-29 (turno 10 — bloque ⑯ historial real + revisión de flujo + limpieza de mockups + commit). Doc para **retomar la construcción** sin re-descubrir.
> Brief original: `PROMPT-APP-FLUTTER.md` (mismo folder). Mockups (inspiración): `mockups/`.
> Proyecto Flutter: `C:\Codigos\nenis-app\nenis_app`. Backend: `C:\Codigos\sellgeneral-api`.

## Cómo retomar (resumen de 1 minuto)
- Es la app **nativa de la compradora** (Flutter, iOS+Android), multi-tenant: un `Account` global le compra a muchas tiendas. Backend en `sellgeneral-api` (.NET, ya en `main` con Fase 0/1).
- Método: **full-stack pantalla por pantalla**. Cada bloque: backend → `dotnet build` + `dotnet test` verde; Flutter → `flutter analyze` 0 issues.
- Los **mockups son solo inspiración** (colores/tipografía/layout/componentes). **NUNCA** clonar el chrome del teléfono (barra de estado falsa, bezel). Usar `SafeArea` + chrome real del OS. (Ya se borró el widget `NeniStatusBar`.)
- Lealtad se llama **"Puntos"**. OTP de prueba: **000000**. Logo de la app: **pendiente de generar**.

## ✅ Hecho y validado

### Backend (`sellgeneral-api`) — soporte de compradora agregado
- **OTP modo DEV**: `AuthController.phone/verify` acepta el código DEV (`Auth:DevOtpCode`, default `000000`, solo en Development), hace upsert de la `Account` por teléfono y devuelve `LoginResponse`. `phone/request-otp` avisa `devMode`. (+4 tests)
- **Feed de Home**: `GET /api/me/home` → `BuyerController` + `BuyerFeedService` + DTOs (`BuyerHomeDto/BuyerStoreDto/BuyerActiveOrderDto/BuyerRecentOrderDto`). Lee **cross-tenant por `AccountId`** (`IgnoreQueryFilters` + scoping por AccountId, patrón de `ClientClaimService`): tiendas (marca+puntos), total de puntos (`Client.CurrentPoints`), pedido activo (Pending/Confirmed/Shipped/InRoute) y recientes. `liveCount`/`isLive` = 0 (stub). Registrado en `Program.cs`. (+3 tests, incluye aislamiento)
- **Mis pedidos (⑨)**: `GET /api/me/orders?filter=all|open|closed&businessId=&page=&pageSize=` (default `all`, `page=1`, `pageSize=20`, max 50). `BuyerController` + nuevo `IBuyerOrdersService` + DTOs (`BuyerOrderDto`, `BuyerOrdersResponse` con `Total/Page/PageSize/Filter/BusinessId`, `BuyerOrderFilter` con `IsValid()`). Mismo patrón cross-tenant (`IgnoreQueryFilters` + scoping por `AccountId`). Filtro `open` agrupa Pending/Confirmed/Shipped/InRoute; `closed` agrupa Delivered/Canceled/NotDelivered/Postponed. `businessId` opcional para acotar a una tienda. `pageSize` se clampa a 1..50. Registrado en `Program.cs`. **+9 tests** (orden, filtros open/closed, paginación, `businessId`, aislamiento entre cuentas, sin claims, clamp de pageSize, filtro inválido cae a `all`).
- **Puntos (⑫)**: `GET /api/me/rewards` → `IBuyerRewardsService` + DTOs (`BuyerRewardDto`, `RewardsByBusinessDto`). Cross-tenant por `AccountId`. Devuelve catálogo de `LoyaltyReward` activos **de las tiendas donde la compradora está reclamada** (un Client con su `AccountId`), junto con `StorePoints` por tienda. Tiendas sin catálogo vienen con `rewards` vacío. Ordena tiendas por puntos desc y rewards por `SortOrder, PointsCost`. Registrado en `Program.cs`. **+6 tests** (sin claims, sin rewards, agrupado por tienda, orden por puntos, aislamiento entre cuentas, fallback de color). Reusa del backend existente: modelo `LoyaltyReward` + `LoyaltyRewardType` (FixedDiscount/FreeShipping/Gift) y seeder de ejemplo (`DevelopmentTenantSeeder.EnsureLoyaltyRewardsAsync`).
- **Mi cuenta (⑮)** = **frontend only** (todos los endpoints ya existían): reusa `GET /api/client-claims/mine` (Devuelve `ClaimedClientSummaryDto`: `ClientId/BusinessId/BusinessName/ClientName/LinkedBy/ClaimedAt`; cross-tenant por `AccountId`, sin teléfono ni dirección por privacidad). NO agrega nada al backend en este turno. `dotnet test` se mantiene en **135/135**.
- **Rastreo (⑩)** = **frontend only** (todo lo necesario ya existía en backend): reusa `GET /api/pedido/{accessToken}` (público, devuelve `ClientOrderView` con ``; el controller además transforma `Status="InTransit"` cuando el chofer viene en camino) + `DeliveryHub.JoinOrder(accessToken)` (que automáticamente se suscribe al grupo `t{BusinessId}_Order_{accessToken}` para `DeliveryUpdate` y, si la tienda tiene `LiveGpsTracking`, también al grupo `t{BusinessId}_Tracking_{driverToken}` para `LocationUpdate`). NO agrega nada al backend en este turno. `dotnet test` se mantiene en **135/135**.
- **Tandas (⑬) + Sorteos (⑭)** = **full-stack**. Modelos ya existían (`Tanda`/`TandaParticipant`/`TandaPayment`/`TandaProduct` y `Raffle`/`RaffleParticipant`/`RaffleEntry`/`RaffleDraw`); los controllers admin (`TandaController`, `RafflesController`) solo servían al lado vendedor. Crea 2 nuevos endpoints cross-tenant por `AccountId`:
  - `GET /api/me/tandas` → `IBuyerTandasService.GetMyTandasAsync(accountId)` → lista `MyTandaDto` con `TandaId/BusinessId/BusinessName/BrandPrimaryColor/ClientId/Name/ProductName/TotalWeeks/WeeklyAmount/StartDate/Status/CurrentWeek/IsMine/MyTurn/HasPaidThisWeek/PaidWeeks/AmIThisWeekWinner`. Calcula `CurrentWeek = clamp((today - StartDate) / 7d + 1, 1, TotalWeeks)`. `AmIThisWeekWinner = (MyTurn == CurrentWeek)`. `HasPaidThisWeek` y `PaidWeeks` derivan de `TandaPayment.IsVerified`. Une las tandas activas/draft/completed de las tiendas donde la compradora tiene Client, más las que ya son suyas. **+9 tests** (sin claims, claims sin tandas, disponible sin participación, participación con pago verificado, pago no verificado, no-ganador-esta-semana, aislamiento, cancelada excluida, fallback de color).
  - `GET /api/me/raffles` → `IBuyerRafflesService.GetMyRafflesAsync(accountId)` → lista `MyRaffleDto` con `RaffleId/BusinessId/BusinessName/BrandPrimaryColor/ClientId/Name/ImageUrl/PrizeType/PrizeValue/PrizeDescription/RaffleDate/Status/TandaName/MyEntryCount/IsMineEntered/AmIWinner/AnnouncedAt`. Excluye sorteos en `Draft` (no publicados). `MyEntryCount` cuenta `RaffleEntry` por orden elegible; `AmIWinner` se marca si algún `RaffleParticipant.IsWinner = true` o si el `WinnerId` apunta a uno de los `Client` de la compradora. **+8 tests** (sin claims, claims sin sorteos, activo sin entradas, con entradas, ganador en `Completed`, `Draft` excluido, aislamiento, fallback de color).
- **Tienda de vendedora (⑦)** = **full-stack**. Crea un nuevo endpoint single-shot para alimentar la pantalla `/store/{businessId}`:
  - `GET /api/me/store/{businessId}` → `IBuyerStoreService.GetStoreAsync(accountId, businessId)` → `BuyerStoreDetailDto` con `BusinessId/Name/Slug/City/LogoUrl/BrandPrimaryColor/BrandAccentColor/ClientCount/IsVerified`, `Points: { CurrentPoints, NextRewardAt }`, `Live?: { SessionId, Title, ViewerCount, Topics, ProcessedAt }`, `Products: List<BuyerProductDto>`, `ActiveTandasCount`, `ActiveRafflesCount`. Lanza `StoreNotFoundException` (→ 404) si la tienda no existe o si la compradora no tiene un Client reclamado en ella. `IsVerified` = heurística barata (logo + color de acento presentes). `NextRewardAt` = `LoyaltyReward.PointsCost` mínimo activo. `Live` = `LiveSession.Status == Ready` con `Topics` de los primeros 3 `LiveProduct.Keyword` ordenados por `AnnouncedAtSeconds`. `Products` = top 24 `Product.IsActive` ordenados por Id desc (el modelo `Product` no tiene imágenes — placeholder con gradient en la app). `ActiveTandasCount` cuenta `Status in (Active, Draft)`. `ActiveRafflesCount` cuenta `Status == Active`. **+10 tests** (404 sin business, 404 sin client, happy path, nextRewardAt = más barata activa, live con topics, solo Ready se devuelve, top 24 productos, counts de actividad, aislamiento entre cuentas, IsVerified heurística).
- **Confirmación de pedido apartado (⑪)** = **full-stack**. No existía ningún endpoint del lado compradora para apartar un producto (los `Order` se creaban desde el panel admin o el POS, y la compradora nunca creaba orders). Se modela el apartado como un `Order` con `Status = Pending`, `OrderType = PickUp`, `Tags = "apartado"`, sin decrementar stock (el `PosService.PayPosOrderAsync` lo sigue decrementando al pagar en tienda):
  - `POST /api/me/reserve` con body `ReserveProductRequest(BusinessId, ProductId, Quantity = 1)` → `IBuyerReserveService.ReserveAsync(accountId, request)` → reusa `BuyerOrderDto` como response (la app navega a `/tracking/{id}?token={accessToken}` con el `AccessToken` recién creado). Lanza `ReserveNotFoundException` (→ 404) si la tienda no existe, si la compradora no tiene Client en ella, o si el producto no existe/pertenece a otra tienda. Lanza `ReserveBadRequestException` (→ 400) si el producto está inactivo, sin stock suficiente, o cantidad < 1. Notifica a la vendedora vía `IPushNotificationService.SendNotificationToAdminsAsync` (envuelto en try/catch para que un fallo de push no tumbe la operación). **+8 tests** (404 sin client, 404 producto de otra tienda, 400 inactivo, 400 stock insuficiente, 400 quantity < 1, happy path con `Status=Pending`/`OrderType=PickUp`/`Tags=apartado`/`AccessToken=GuidN`/`Items.Count=1`, stock NO se decrementa al apartar, aislamiento entre cuentas).
- **Notificaciones / Pagos / Direcciones (⑯⑰⑱)** = **mixto**. ⑯ frontend-only (no hay endpoint de historial — el backend solo emite Web Push pero no persiste); ⑰⑱ full-stack. Se crean 2 endpoints nuevos cross-tenant por `AccountId`:
  - `GET /api/me/payments` → `IBuyerPaymentService.GetMyPaymentsAsync(accountId)` → `List<BuyerPaymentDto>` con `PaymentId/OrderId/BusinessId/BusinessName/BrandPrimaryColor/LogoUrl/Amount/Method/Date/RegisteredBy/Notes/OrderStatus/OrderTotal`. Trae todos los `OrderPayment` de los `Order` de los `Client` de la Account, ordenados por fecha desc, con info del Order y la tienda para evitar N+1. **+5 tests** (sin claims, claims sin pagos, happy path cross-business, aislamiento entre cuentas, solo pagos de SUS pedidos no de los de otras clientas de la misma tienda).
  - `GET /api/me/addresses` → `IBuyerAddressService.GetMyAddressesAsync(accountId)` → `List<BuyerAddressDto>` con `ClientId/BusinessId/BusinessName/BrandPrimaryColor/LogoUrl/Address/Latitude/Longitude/DeliveryInstructions` (un registro por Client reclamado, ordenado por BusinessName). **+3 tests** (sin claims, happy path, aislamiento).
  - `PUT /api/me/addresses/{clientId}` con body `UpdateBuyerAddressRequest(Address?, Latitude?, Longitude?, DeliveryInstructions?)` → `IBuyerAddressService.UpdateAddressAsync(accountId, clientId, request)` → actualiza el `Client` y devuelve el DTO. Solo se modifican los campos no-null; string vacío = borrar. 404 si el Client no pertenece a la Account. **+4 tests** (404 client ajeno, 404 client inexistente, happy path, solo toca campos enviados, string vacío borra).
- **Notificaciones — historial real (⑯ full-stack)**. Se crea el modelo `Notification` (ITenantOwned) con migración EF Core `AddNotifications` y se modifica `PushNotificationService.SendNotificationToClientAsync` para que también persista cada push en la DB (failure-safe con try/catch). Se crea `IBuyerNotificationService` con:
  - `GET /api/me/notifications` → `List<BuyerNotificationDto>` (Id, BusinessId, BusinessName, BrandPrimaryColor, Title, Message, Tag, Url, OrderId, CreatedAt, ReadAt) cross-tenant, top 200 desc. **+8 tests** (sin claims, aislamiento, MarkAsRead happy path / 404 client ajeno / 404 inexistente, MarkAllAsRead solo mis no leídas, CountUnread).
  - `POST /api/me/notifications/{id}/read` (NoContent) marca una.
  - `POST /api/me/notifications/read-all` devuelve `{updated: N}`.
  - `GET /api/me/notifications/unread-count` lo usa el badge del icono 🔔 del Home.
- **Validación**: `dotnet build` 0 errores; `dotnet test` **190/190** (182 previos + 8 Notifications).
- **Ya existían y se reúsan**: `/api/client-claims/{candidates, by-phone/{id}, by-order-token/{token}, mine}`, `/api/loyalty/{clientId}` y `/api/loyalty/rewards` (este último tenant-scoped, no aplica a la compradora cross-tenant), la vista pública por token `GET /api/pedido/{accessToken}`, los hubs SignalR `/hubs/delivery` y `/hubs/tracking`, los modelos + controllers admin de Tandas/Sorteos, los providers Flutter `tandasControllerProvider` y `rafflesControllerProvider` (reusados en los tabs de Tandas/Sorteos de la pantalla de tienda, filtrando client-side por `businessId`), `IOrderService.CalculateOrderDates` (reusado para calcular `ExpiresAt`/`ScheduledDeliveryDate`), `IPushNotificationService` (ahora también persiste), `BuyerOrderDto` (reusado como response, contiene `AccessToken` listo para `/tracking`), el modelo `Client` con `Address/Latitude/Longitude/DeliveryInstructions` (reusado para el bloque ⑱), el `Client`/`OrderPayment` con sus propiedades y la convención de los `Method` (Efectivo/Tarjeta/Transferencia/Deposito), `Client` con `AccountId` para validar la pertenencia de la `Notification` en el service.

### Flutter (`nenis_app`)
- **Bloques 1 y 2** (sesión previa): proyecto + deps (Riverpod 3, go_router, dio, secure_storage, google_fonts, material_symbols, google_maps, signalr, intl), sistema de diseño `core/theme/` (+ `brand_theme.dart` = tema por tienda en runtime), widgets compartidos, style gallery.
- **Integración (`lib/core/`)**: `config/app_config.dart` (base URL + mapeo emulador: Android `10.0.2.2:5080`, iOS/desktop `localhost:5080`), `auth/session.dart` (modelos), `storage/session_storage.dart` (secure storage), `api/dio_provider.dart` (Dio + interceptor `Bearer` + `X-Business-Id`, omite rutas públicas), `auth/auth_repository.dart` (OTP/email), `auth/auth_controller.dart` (`AsyncNotifier`: carga sesión al arrancar, login/logout/negocio activo), `router/app_router.dart` (redirect por auth; OTP de compradora sin negocio → `/claim`), `utils/color_hex.dart`.
- **Pantallas reales (de los mockups, sin chrome falso)**:
  - **Login** (`features/auth/screens/login_screen.dart`) — teléfono → `requestOtp`; Facebook ("pronto"); "Acceso de equipo" (correo legacy → `/auth/login`).
  - **OTP** (`otp_screen.dart`) — 6 celdas, reenvío con timer, chip "000000", `verifyOtp`.
  - **Reclamar perfil** (`claim_profile_screen.dart` + `features/claim/data/`) — candidatos reales, seleccionar/reclamar, estados vacío/error.
  - **Home** (`features/home/screens/home_screen.dart` + `features/home/data/`) — hero del pedido temado, bento Puntos/Lives, Mis tiendas, recientes, nav flotante, pull-to-refresh, vacío→banner "Reclama tu historial".
  - **Mis pedidos ⑨** (`features/orders/screens/orders_screen.dart` + `features/orders/data/`) — header "Mis pedidos", `SegmentedControl` (Todos / En curso / Cerrados), lista paginada con `RefreshIndicator`, paginador Anterior/Siguiente, empty state con CTA "Explorar tiendas", error con reintentar, `GlassBottomNav` montado (el ítem del nav ya estaba en `buildDefaultNavItems()`). Tap en fila → `/tracking/{id}`. Filtro mapea a query `filter=all|open|closed`; reset a página 1 al cambiar filtro. Controller = `OrdersController` (Riverpod `AsyncNotifier<BuyerOrdersPage>`) con `setFilter/nextPage/prevPage/setBusiness`.
  - **Puntos ⑫** (`features/points/screens/points_screen.dart` + `features/points/data/`) — header "Tus puntos", hero card con total global (icono `stars` gold, número display 38px, "en N tiendas"), sección "Tus tiendas" con `_StorePointsRow` (avatar con color de marca + "{puntos} puntos acumulados aquí · canjea desde {min} pts" + chevron → `/store/{id}`), sección "Premios que puedes canjear" con un sub-header por tienda + carousel horizontal de `_RewardCard` (emoji del backend, nombre, chip "{pts} pts", botón "Canjear" con snackbar "El canje se aplica al pagar en tu pedido" — el canje real es en el flujo de cobro del vendedor, `POST /api/loyalty/redeem`). Empty state → "Reclamar mi perfil" → `/claim`. `GlassBottomNav` montado. Feed = `pointsFeedProvider` (Riverpod `FutureProvider.autoDispose`) contra `/api/me/rewards`.
  - **Mi cuenta ⑮** (`features/account/screens/account_screen.dart` + `features/account/data/`) — header "Mi cuenta" + sub, card de perfil con `UserAvatar` + `displayName` (o "Agrega tu nombre" si es "Clienta") + "+52 ··· XX" (teléfono enmascarado) + chip "Verificado", sección "TUS TIENDAS" con la lista de `_ClaimedStoreRow` (avatar con paleta determinística por `businessId` + nombre + `linkedByLabel` + "Vinculada el {fecha}") con `Divider` entre filas, empty state, error con reintentar, banner "Próximamente" (editar nombre, desvincular, notificaciones), botón `PillButton(ghost)` "Cerrar sesión" con `AlertDialog` de confirmación que llama `authControllerProvider.notifier.logout()` (el redirect del router lleva a `/login` solo). `GlassBottomNav` montado. Feed = `myClaimedClientsProvider` contra `/api/client-claims/mine`.
  - **Rastreo ⑩** (`features/tracking/screens/tracking_screen.dart` + `features/tracking/data/`) — ruta `/tracking/:orderId?token=…`. Pantalla con `DraggableScrollableSheet`: top bar con back + chip "Pedido #XX · Rastreo en vivo" + dot de estado; **mapa placeholder** (320px) con `CustomPaint` que pinta calles, parques y agua, 3 markers (tienda rosa fuerte, chofer con halo rosa, casa ink) y 2 trazos (ruta planificada punteada rosa, ruta recorrida sólida). Para pedidos sin ruta activa / terminales (`delivered`, `notDelivered`, `canceled`) o en preparación, el centro del mapa muestra un mensaje grande con icono. **Bottom sheet dragable** con: handle, header con `etaLabel` (H1) + subtítulo del status + chip del estado (4 colores), driver row (avatar lavanda "V" + "Vicente" + "Tu repartidor · a N paradas de ti" + botones chat/llamar), **timeline 4 pasos** con dots (done=verde, active=rosa con halo, todo=gris) y línea vertical, dirección del cliente si existe, y lista de items del pedido con totales. `RefreshIndicator` para re-pegar el GET. **SignalR** (signalr_netcore 1.4.4): `TrackingHubClient` se conecta a `/hubs/delivery` con `withAutomaticReconnect(retryDelays: [0,2,5,10,30]s)`, `start()` y `joinOrder(token)`; emite `LocationUpdate` y `DeliveryUpdate` por streams. `TrackingController` (Riverpod `AsyncNotifier<OrderTracking?>` + `TrackingToken` Notifier para el token) hace el GET inicial y se suscribe a los streams para actualizar el `state` con `copyWith`. El token se pasa por query string desde 3 callers (home hero activo, orders row, y actualizado de `home_screen.dart` recientes a `/orders` ya que `BuyerRecentOrder` no trae `accessToken`).
  - **Tandas ⑬** (`features/tandas/screens/tandas_screen.dart` + `features/tandas/data/`) — header "Tusandas" + sub, `SegmentedControl` con 2 tabs ("Mis tandas" / "Disponibles"), lista de `_TandaCard` con avatar de marca + nombre tienda + nombre de la tanda + producto + meta (turno + semana) + `_PaymentStrip` de dots (verdes=pagado / gris=pendiente) + chip de estado de pago semanal + monto semanal. Si `AmIThisWeekWinner`, badge "¡Te toca!" con gradient. Si la tanda NO es mía, footer con icono de grupo + "X de Y semanas" + "Pregunta en tu tienda para inscribirte". Empty state con CTA "Explorar tiendas" → `/home`. `GlassBottomNav` montado. Filtro se aplica en cliente sobre la lista que devuelve `GET /api/me/tandas`. Feed = `tandasControllerProvider` (Riverpod `AsyncNotifier<List<BuyerTanda>>` con `setFilter`).
  - **Sorteos ⑭** (`features/raffles/screens/raffles_screen.dart` + `features/raffles/data/`) — header "Sorteos" + sub, `SegmentedControl` con 3 tabs ("Activos" / "Mis boletos" / "Historial"), lista de `_RaffleCard` con thumbnail (gradient de marca) + nombre tienda + nombre del sorteo + premio + fecha de sorteo + chip "X boletos" (o "Aún sin boletos") + status chip (Activo/Finalizado/Cancelado). Si `AmIWinner`, borde verde + badge "¡Ganaste!". Empty state con icono y copy específicos por tab. `GlassBottomNav` montado. Filtro se aplica en cliente sobre la lista que devuelve `GET /api/me/raffles`. Feed = `rafflesControllerProvider` (Riverpod `AsyncNotifier<List<BuyerRaffle>>` con `setFilter`).
  - **Tienda de vendedora ⑦** (`features/store/screens/store_screen.dart` + `features/store/data/`) — ruta `/store/:businessId` (ya registrada). `ConsumerStatefulWidget` setea el `businessId` en `storeBusinessIdProvider` en `initState`. Layout: **header gradient brand** (132px, radius inferior 28) con badge "TIENDA" + 3 iconos circulares blancos (back / favorite / share, los 2 últimos con snackbar "Pronto"); **profile row** (margin-top -34) con `StoreAvatarXl` (74x74 con border 4px blanco en caja circular) + nombre 20px + icono verificado si `IsVerified` + subtitle "{city} · {N} clientas" + `PillButton(brand, "Seguir")`; **ptsbar** con gradient `#FFF6E6 → #FFFFFF` e icono stars en caja `#FFF2D4`, "{N} puntos" + "acumulados aquí · canjea desde X pts" (tap → `/points`); **live-strip** opcional (gradient rosa fuerte) con icono sensors + "{title} en vivo" + topics (3 keywords del LiveProduct) + botón blanco "Entrar" (tap → `/live/{sessionId}`); **tabs row** scrollable con counts (Productos · 24, En vivo · 1, Tandas · 3, Sorteos · 2); **contenido por tab**: Productos = grid 2 cols con `_ProductCard` (placeholder gradient con icono imagen + nombre + precio + botón `+` que ahora hace `context.push('/reserve/$businessId/$productId')`), Lives = card con info del live + badge "LIVE" o empty state, Tandas = reusa `tandasControllerProvider` filtrando por `businessId` con `_TandaCompactRow`, Sorteos = reusa `rafflesControllerProvider` filtrando por `businessId` con `_RaffleCompactRow`. Empty state específico por tab. `GlassBottomNav` montado con `currentRoute: '/store/$businessId'`. Models: `BuyerStoreDetail` + `StorePoints` + `BuyerLiveSummary` + `BuyerProduct` con `fromJson`. State: `storeBusinessIdProvider` (`Notifier<int?>`) + `storeSelectedTabProvider` (`Notifier<StoreTab>`) + `storeControllerProvider` (`AsyncNotifier<BuyerStoreDetail?>`).
  - **Confirmación de pedido apartado ⑪** (`features/reserve/screens/reserve_screen.dart` + `features/reserve/data/`) — ruta `/reserve/:businessId/:productId`. `ConsumerStatefulWidget` setea el `storeBusinessIdProvider` y reusa `storeControllerProvider` para resolver el `BuyerProduct` (404 si el producto no está en la lista o no hay stock). Layout: header "Apartar producto" + sub, card de producto con gradient marca + nombre + precio + "{N} disponibles", stepper de cantidad con `_StepperButton` circulares y validación `1 ≤ qty ≤ stock`, card de resumen con subtotal formateado, 2 botones: "Confirmar apartado" (`PillButton.brand` con icono `bookmark`, deshabilitado si `!inStock` o durante submit) y "Cancelar" (ghost). Al confirmar, llama `ReserveRepository.reserve(...)` y abre `_SuccessDialog` con check verde + "¡Apartado!" + subtítulo + Pedido #XX + 2 botones: "Ver mi pedido" → `/tracking/{id}?token={accessToken}` (auto-navega al rastreo del nuevo order) o "Seguir comprando" → `context.pop()` (vuelve a la tienda). Errors: snackbar con mensaje del backend (404 producto no disponible / 400 sin stock). Models: `ReserveRequest` con `toJson()` y `typedef ReserveResult = BuyerOrder` (reusa el modelo existente con `accessToken` listo para tracking). Repository: `ReserveRepository.reserve(req)` con manejo de 404/400/500 → `ReserveException(message)`.
  - **Notificaciones ⑯** (`features/notifications/screens/notifications_screen.dart` + `features/notifications/data/`) — ruta `/notifications`. **Historial real** (turno 10). `ConsumerStatefulWidget` con `NotificationsFilter { all, unread }` interno (no se vuelve a pedir al backend). Layout: header "Notificaciones" + sub, `SegmentedControl` Todas/No leídas, lista cronológica de `_NotificationRow` con `icon` derivado del `tag` (`delivered` → `celebration`, `driver-en-route` → `local_shipping`, `driver-nearby` → `location_on`, `chat-driver` → `chat`, `order-confirmed` → `check_circle`, `card-payment` → `credit_card`, `reserve` → `bookmark`, default `notifications`). Estado "leída" con texto `ink2` y sin borde; "no leída" con texto bold `ink` + borde color de marca + dot rosa. Formato de tiempo relativo: "hace X min/h/d" o "d MMM" si >7d. Empty state contextual: si filtras "No leídas" y no hay → "¡Al día! No tienes notificaciones sin leer"; si filtras "Todas" y no hay → "Aún no tienes notificaciones". Al hacer tap, marca como leída (optimistic + `POST /me/notifications/{id}/read`) y hace deep link si la `URL` es interna (`/tracking/...`, `/store/...`, etc.). Pill "Marcar todas como leídas" arriba de la lista si hay no leídas; llama `POST /me/notifications/read-all` y muestra snackbar. Feed = `notificationsFeedProvider` (`FutureProvider.autoDispose<List<BuyerNotification>>`). Badge 🔴 en el icono de notifications del Home: `_NotificationsIconButton` con `Stack` que envuelve `PillIconButton` y un `Positioned` con `Container(backgroundColor: AppColors.liveRed, border)` cuando `unreadNotificationsCountProvider` devuelve > 0; muestra "9+" si >9; se rehidrata al volver de la pantalla.
  - **Limpieza de mockups (turno 10)** — Se eliminó `lib/features/dev/style_gallery_screen.dart` y la carpeta `features/dev/` entera (era un mockup puro de preview de estilos). Se quitó la ruta `/style-gallery` del `app_router.dart` y su import. Se actualizó `lib/features/live/screens/live_screen.dart` para que en vez del placeholder "(placeholder)" muestre un mensaje honesto "La experiencia de live shopping está en construcción" con icono `sensors` en gradient rosa + CTA "Volver al inicio". El screen sigue montado en la ruta `/live/:sessionId` (los entry points — `store_screen.dart:332` y `notifications_screen.dart` con deep link desde el tag de live — siguen funcionando, pero ahora muestran un estado honesto en vez de un placeholder).
  - **Mis pagos ⑰** (`features/payments/screens/payments_screen.dart` + `features/payments/data/`) — ruta `/payments`. Layout: header "Mis pagos" + sub, card de resumen con gradient `#FFF7E6` + icono `payments` gold + "Total pagado" (formateado `es_MX`) + pill con "$N pagos", lista cronológica descendente de `_PaymentRow` con `StoreAvatarSm` (gradient de marca) + "Pago #ID" + "{tienda} · {fecha}" + `_MethodChip` con color por método (Tarjeta = route blue, Transferencia/Deposito = pending amber, Efectivo = delivered green) + monto a la derecha. Empty state con icono `payments` gold. Feed = `paymentsFeedProvider` (`FutureProvider.autoDispose<List<BuyerPayment>>`).
  - **Mis direcciones ⑱** (`features/addresses/screens/{addresses_screen,address_edit_screen}.dart` + `features/addresses/data/`) — rutas `/addresses` y `/addresses/:clientId`. Pantalla lista: header + sub, una `_AddressRow` por tienda con avatar de marca + nombre + "Editar" (`PillButton.brand, expand=false`) + 3 líneas de info (`location_on` para calle, `my_location` para lat/lng formateados a 4 decimales, `info` para referencias). Pantalla edición: header con back + "Editar dirección" + "Tienda: {businessName}" + card con brand del negocio, formulario con 4 campos (`AppTextField`): Calle y número (`maxLines: 2`), Referencias (`maxLines: 2`), Latitud y Longitud (numberWithOptions decimal+signed, lado a lado). Al confirmar, envía solo los campos que cambiaron vs el original (vía `UpdateAddressRequest.toJson()` que omite nulls y usa string vacío para borrar). Snackbar "Dirección guardada." al éxito, pop a la lista, invalida el feed. Models: `BuyerAddress` con `hasAddress` getter + `UpdateAddressRequest` con flags `clearX` para borrar explícitamente. Repository: `AddressesRepository.getMyAddresses()` y `updateAddress(clientId, req)` con manejo de 404.
  - **AppTextField extendido** — `lib/shared/widgets/app_text_field.dart` ahora acepta `label` (opcional, renderiza el label arriba con `subtitle w600 ink2` y el campo abajo) y `maxLines` (default 1; cuando > 1 el Container se ajusta al contenido y permite multilínea). Retrocompatible: los 4 callers existentes (`search_screen`, `claim_profile_screen`, `login_screen`, `otp_screen`) siguen funcionando con el constructor sin esos parámetros.
- **Flujo real completo**: Login → OTP → Reclamar → **Home con datos en vivo** → **Notificaciones (empty state por ahora, desde el icono 🔔 del Home)** → **Mis pedidos (Todos/En curso/Cerrados) paginado y con bottom nav** → **Puntos (total + por tienda + catálogo de premios) con bottom nav** → **Mi cuenta (perfil + tiendas vinculadas + 3 cards de Pagos/Direcciones/Notificaciones + logout) con bottom nav** → **Rastreo de pedido (mapa + driver + timeline + items, con SignalR en vivo, con bottom nav) desde el hero activo del home o cualquier fila de Mis pedidos** → **Tandas (Mis tandas / Disponibles) con bottom nav** → **Sorteos (Activos / Mis boletos / Historial) con bottom nav** → **Tienda de vendedora (Productos / Lives / Tandas / Sorteos con bottom nav) desde el hero de tienda del home o el row de Puntos** → **Apartar producto (modal de éxito → rastreo del nuevo order) desde el `+` de cualquier producto en la tienda** → **Historial de pagos (cronológico por tienda) desde Mi cuenta → Mis pagos** → **Edición de direcciones (formulario con 4 campos) desde Mi cuenta → Mis direcciones → Editar**.
- `flutter analyze` → **No issues found**.

## ⛏️ Pendiente (lo que falta crear)

Las pantallas siguen en **placeholder** salvo las de arriba. Cada una = **pantalla (del mockup) + su endpoint backend** (full-stack):

| # | Pantalla | Endpoint backend que falta |
|---|---|---|
| ⑧ | **Live shopping** (viewer real) | endpoints de lives (complejo) + SignalR |
| ⑯ (historial real) | Persistir `Notification` con `ReadAt` y exponer `GET /api/me/notifications` | (frontend-only ya implementado) |
| ⑱ (multi-dirección) | Modelo `ClientAddress` con FK a `Client` + migración | (1 dir por Client ya implementado) |
| ①② | Splash (mejorar) · Bienvenida (onboarding) | — |

**Sugerencia de orden:** ahora ⑧ Live (viewer en tiempo real con SignalR) y al final ①②.

**Otros pendientes:** `liveCount`/`isLive` reales (hoy 0), logo de la app, Facebook/SMS reales (hoy stub/DEV).

## Cómo correr / validar
- Backend dev: en `C:\Codigos\sellgeneral-api` → `ASPNETCORE_ENVIRONMENT=Development dotnet run` (escucha en `:5080`). Requiere `appsettings.Development.json` (gitignored, connection string real).
- Tests backend: `dotnet test Tests\EntregasApi.Tests\EntregasApi.Tests.csproj` (si falla la copia de `runtimeconfig`, primero `dotnet build` del test project y luego `dotnet test --no-build`).
- Flutter: en `C:\Codigos\nenis-app\nenis_app` → `flutter analyze` y `flutter run` (emulador). Override de URL: `--dart-define=API_BASE_URL=http://host:puerto`.

## Decisiones tomadas (no re-litigar)
- App = compradora; estética bloqueada (tema por tienda; tokens en `mockups/styles.css`); "Puntos"; login sin ilustración.
- Backend de comprador: cross-tenant por `AccountId` con `IgnoreQueryFilters` + scoping explícito (ver `BuyerFeedService`/`ClientClaimService`). Auth de comprador = `[Authorize]` con JWT (sub=AccountId), **sin** membership ni `X-Business-Id`.
- Mockups = inspiración, NO clonar chrome de teléfono.

## Estado git
**Sin commitear** en AMBOS repos:
- `sellgeneral-api`: `AuthController` (OTP DEV) + `BuyerController` (home + orders + rewards) + `BuyerTandasController` + `BuyerRafflesController` + `BuyerStoreController` + `BuyerReserveController` + `BuyerPaymentController` + `BuyerAddressController` + `BuyerFeedService` + `BuyerOrdersService` + `BuyerRewardsService` + `BuyerTandasService` + `BuyerRafflesService` + `BuyerStoreService` + `BuyerReserveService` + `BuyerPaymentService` + `BuyerAddressService` + DTOs en `Dtos.cs` (incluye `BuyerOrderFilter`/`BuyerOrderDto`/`BuyerOrdersResponse`/`BuyerRewardDto`/`RewardsByBusinessDto`/`MyTandaDto`/`MyRaffleDto`/`BuyerProductDto`/`BuyerLiveSummaryDto`/`BuyerStorePointsDto`/`BuyerStoreDetailDto`/`ReserveProductRequest`/`BuyerPaymentDto`/`BuyerAddressDto`/`UpdateBuyerAddressRequest`) + registro en `Program.cs` + 9 archivos de tests nuevos (`BuyerFeedServiceTests`, `BuyerOrdersServiceTests`, `BuyerRewardsServiceTests`, `BuyerTandasServiceTests`, `BuyerRafflesServiceTests`, `BuyerStoreServiceTests`, `BuyerReserveServiceTests`, `BuyerPaymentServiceTests`, `BuyerAddressServiceTests`).
- `nenis_app`: todo `lib/core/{config,api,auth,storage,utils,router}`, `lib/features/{auth,claim,home,store,orders,points,account,tracking,tandas,raffles,reserve,notifications,payments,addresses}/...`, y la baja de `lib/shared/widgets/status_bar.dart`.
